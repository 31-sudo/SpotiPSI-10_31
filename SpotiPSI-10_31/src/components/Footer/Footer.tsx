import useStyles from "./FooterStyles";

const Footer:React.FC = () => {
    const { classes } = useStyles();
    return(
        <div className={classes.footer}>נגן מasdsadasd</div>
    );
}
export default Footer;